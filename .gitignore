# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Python backend
.venv/
__pycache__/
*.pyc

# Cached NBA responses are keyed by player and season, so browsing the app
# accumulates them locally. Only the default subject is committed, since it
# is the offline fallback that keeps the dashboard working when
# stats.nba.com is unreachable.
server/cache/*
# The featured subjects ship in full -- shots, splits and the generated
# summary -- so the whole app, AI panel included, works offline for anyone
# who clones this without an API key, and so a hosted demo can serve them
# with SHOTIQ_SUMMARY_LIVE left off.
#
# One line per subject rather than three: the wildcard covers shots-,
# splits- and summary- while still naming the exact player and season, so
# no other locally cached player is swept in by accident. Roughly 3 MB in
# total, which is the price of a demo that needs no network and no key.
!server/cache/*-1628983-2025-26.json
!server/cache/*-203999-2021-22.json
!server/cache/*-201939-2016-17.json
!server/cache/*-2544-2015-16.json
!server/cache/*-1629029-2023-24.json

# Environment — never commit real keys
.env
.env.*
!.env.example

# Terraform
#
# Added before terraform/ exists, deliberately. State is written on the very
# first `terraform apply`, and a state file committed once stays in the
# history whether or not it is deleted later.
#
# State is not a build artefact -- it is the record mapping each resource in
# the config to the real object in GCP, and it holds every attribute those
# objects returned, in cleartext, including ones marked sensitive. It is also
# the thing that makes `destroy` possible: lose it and Terraform no longer
# knows what it created, so the resources keep billing with nothing tracking
# them.
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl.bak
crash.log

# Values, not declarations. variables.tf declares what the config takes and is
# committed; .tfvars carries this environment's actual project id and is not.
*.tfvars
*.tfvars.json
!*.tfvars.example

# Kubernetes -- kubeconfig embeds a cluster access token.
kubeconfig
*.kubeconfig

# Personal
handoff-shotiq.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
