# ShotIQ

An interactive dashboard for exploring NBA shot data — shot distance, angle, defender pressure and shot-clock context.

## Requirements

- Node.js 20 or newer

## Run locally

```
npm install
npm run dev
```

Vite prints a local URL when it starts (http://localhost:5173 by default).
